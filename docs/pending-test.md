# 待测试

- 工作流新建初始化待测：点击新建工作流先弹出名称输入框，工作流名称仅允许小写字母和数字，创建后公开 URL 使用 `域名/用户名称/工作流名称`。
- 工作流公开分享页待测：打开 `/{username}/{workflowName}` 时，无密码分享直接展示和画布一致的只读预览，可拖动画布和缩放但不能编辑或运行节点；有密码分享先输入密码，密码输入和进入按钮需要保持间距。
- 工作流 Star / Fork 待测：登录用户可在公开分享页 Star；需要运行时必须 Fork，Fork 弹窗显示 `用户名称/工作流名称`，可选择独立副本或自动更新，自动更新副本在“我的工作流”卡片显示彩色“自动更新”标签。
- 工作流删除确认待测：列表页、详情页删除工作流都必须输入工作流名称，再二次确认，最后确认删除后不会返还新建工作流次数。
- `/canvas` 工作流库待测：导航改为“我的工作流 / 我的分享 / 社区工作流”，桌面端在左侧、手机端固定到底部；顶部删除标题说明，筛选、显示数量、搜索、新建、删除和工作流统计保持同一行横向展示；工作流和社区卡片改为画布快照背景，左键打开，右键弹出修改名称、打开、删除和工作流信息菜单；我的工作流创建、重命名、删除、批量删除仍应正常。
- 社区工作流待测：用户可把自己的工作流以单独社区名称、语言和标签上传为公开社区作品；社区作品无需密码即可只读打开，但仍要求登录鉴权。
- 我的作品同步待测：社区作品保存为发布快照，不自动跟随原工作流更新；当来源工作流更新时间不同，“我的作品”中显示同步入口，输入来源工作流名称确认后才同步快照。
- 社区工作流权限待测：用户只能上传、同步、删除自己的社区作品，不能修改其他用户作品；管理员后台新增“社区工作流”，仅支持封禁并填写原因。
- 社区工作流封禁待测：封禁后作品不再出现在社区列表，作者在“我的作品”中看到封禁原因且无法公开打开；封禁超过 7 天后查询时自动从我的作品中软删除。

- 注册和资料完善用户名称待测：账号密码注册不再把邮箱作为用户名称，注册资料步骤需要填写 3-24 位小写字母或数字用户名称；第三方登录和 MetaMask 首次登录进入资料完善页时会预填建议用户名称，但用户必须手动确认，页面提示当前系统暂不支持修改用户名称。
- 画布助手问答功能待测：画布右侧助手标题移除“未开发”，问答模式调用后台默认文本模型，历史保存到 `canvas_assistant_sessions` 数据库表，默认保留 7 天且可在后台系统设置中调整；用户只能读取、发送和删除自己工作流下的助手对话。
- `/pricing` 套餐页改为参考图样式：顶部仅保留“升级套餐”，删除“简单透明 · 一次性付费”说明，卡片固定展示免费版、Go、Plus、Pro，Plus 使用紫色高亮。
- 提示词库 EvoLinkAI GPT Image 2 图片路径修复待测：`../../images/...` 同步后应保留 GitHub raw 的 `/main/` 分支段，已入库的缺 `/main/` 图片链接在接口返回时也会自动修正。
- 工作流分享状态回显待测：已经生成过分享链接的工作流重新进入画布时，分享弹窗应显示当前分享链接和密码开关，不需要重新生成链接才能复制；如果后端返回 localhost 链接，前端展示和复制时应替换为当前访问域名。
- 管理后台提示词管理新增“中国大陆图片加速”开关；开启后仅 `/zh-CN` 中文页面的提示词封面和预览 GitHub raw 图片会通过 `https://gh-proxy.com/` 访问，其他语言页面和后台保持原始 raw 链接。
- `/model-3d` 页面待测：布局、间距和浅/深色配色已对齐 `/video`、`/image` 创作台，上传参考图、生成、历史、下载和添加到素材入口仍应正常。
- `/canvas` 工作流库页面待测：改为近全屏布局，新增统计侧栏、搜索筛选、扩展工作流卡片信息和节点预览，创建、重命名、删除、批量删除仍应正常。
- 管理后台用户管理待测：侧边栏底部“前往工作流”改为“前往站点首页”；编辑用户弹窗改为用户卡片布局并展示头像，支持单独调整算力点和工作流创建次数。
- 第三方登录配置待测：自定义 OAuth 保存后应同步前台显示和服务端启用状态，后台卡片不再显示“未开启 / 前台隐藏 / 服务端未启用”；MetaMask 支持配置签名网站名称、网站 URL 和 Logo URL，并用于登录签名校验。
- 管理后台左侧导航新增“模型配置”和“邮件配置”独立入口；模型配置集中管理开放模型、默认模型、算力点和模型渠道，邮件配置独立进入 SMTP 与模板配置，系统设置保留通用配置分类。
- 生图、视频和画布节点生成中的 Aivro anime.js 加载动画统一为首页同款描边字标效果待测。
- Docker 镜像构建待测：云存储上传对象编译错误已修复，GitHub Actions 的 Go 构建阶段不应再出现 `no new variables on left side of :=`。
- Docker 镜像仓库名已切换为 `maishanhub/aivro`，CI 推送、docker-compose 和部署文档引用需要按新仓库名验证。
- `/ads.txt` 根路径待测：前端 Next route 会转发到后端 ads.txt 接口，浏览器访问不应再进入 404 页面。
- 云存储新增存储策略待测：后台可选择只使用本地存储、只使用 S3/R2、优先 S3/R2 失败后自动切本地；后端按文件记录的 provider 读取和删除。
- 云存储新增 3D 模型路径模板和保存天数待测；图片、视频、3D 模型临时文件和生成历史默认按各自保存天数写入 `expires_at`。
- AI 请求和上游响应资源限制待测：AI 请求体、图片 JSON 响应、视频内容和 3D 模型下载超过限制时应返回安全错误，避免无上限读入内存。
- 文件存储流式化待测：用户上传使用 multipart 流式读取，视频内容和 3D 模型上游下载先落临时文件再转存，本地/S3/R2/fallback 三种策略均不应整文件长期放入内存。
- AI 队列存储结构待测：`generation_tasks` 只保存 `request_file_id` 和 `response_file_id`，请求/响应体保存为 `cloud_files.file_type=task`；旧任务清理会同步删除任务对象文件。
- 数据库安装更新待测：启动 AutoMigrate 应补齐 `generation_tasks.request_file_id`、`generation_tasks.response_file_id`、删除旧的 `request_body` / `response_body` 大字段并创建新增复合索引，数据库状态页不应再报告缺字段。
- AI 队列调度待测：排队位置改为计数查询，旧任务清理从每秒执行改为约 10 分钟执行一次，排队、完成、取消和结果读取仍应正常。
- 安全修复待测：开启 Turnstile 后，注册提交和 MetaMask 登录提交也必须完成人机验证。
- 管理后台新增“谷歌广告”页面，可配置 AdSense script 代码、全站开关和页面开关；前台只在启用页面加载官方 AdSense Auto Ads 脚本。
- 管理后台谷歌广告页面新增 ads.txt 编辑，保存后网站根路径 `/ads.txt` 输出对应纯文本内容，用于 Google AdSense 网站审核。
- 公开提示词列表接口新增 5 分钟服务端内存缓存和 CDN 缓存响应头，后台保存、删除、批量删除和远程同步提示词后会清空公开缓存。
- EvoLinkAI GPT Image 2 提示词源同步兼容远端新的 Case Markdown 格式、空图片目录和 `**Prompt**:` 写法，避免提示词库同步为空或生成错误封面地址。
- 管理后台实时同步待测：用户、素材、提示词和算力点日志不再沿用 30 秒 React Query 新鲜期，进入页面、切回窗口和恢复网络会重新请求；后台用户权限、套餐、数据库状态和系统设置在路由切换或窗口重新聚焦时会刷新。
- 数据库安装与状态检查待测：Docker Compose 应用容器默认连接内置 PostgreSQL，后台数据库配置页会展示当前实际驱动和脱敏连接地址；数据库缺字段检查扩展为覆盖当前 AutoMigrate 模型全部字段。
- 首页 Aivro anime.js 字标增加组件和页面容器双重居中约束，避免视觉偏移。
- 登录/注册页按深色点状背景重新排版，移除外层卡片框，表单、分段切换、输入框、按钮和第三方登录入口改为参考图样式。
- 登录、注册和 MetaMask 登录过程中的中间动画不再复用旧线稿 SVG，改为新的 Aivro 字母与 Logo anime.js 动画，并保持更快的一轮播放节奏。
- 登录/注册页顶部恢复左侧 Logo、右侧 Aivro 的品牌行，登录/注册分段控件修正上下高度一致；登录提交动画改为复用首页同款 Aivro 线稿动画且不显示 Logo。
- `/pricing` 套餐卡片整体增大，标题、价格、按钮和功能点字号同步放大，背景恢复为深色点状纹理。
- 管理后台系统设置改回手动保存：顶部提供“保存当前页”，主要配置区块提供独立保存按钮；普通字段修改只提示“有修改未保存”，不会再自动提交。
- 邮件配置修复待测：后台保存回填时会保留当前输入的 SMTP 密码，避免密码被隐藏字段清空；SMTP 测试发送增加连接和读写超时，失败时会显示具体 SMTP 错误。
- Google/GitHub/Linux.do OAuth 授权不再被私有“启用服务端”开关单独拦截；后台保存内置登录时会同步前台显示和服务端启用状态，OAuth 编辑弹窗展示可复制的完整授权回调地址，后端识别代理协议头以减少 Google `redirect_uri_mismatch`。
- 用户表 `metamask_address` 明确使用固定数据库列名，服务启动 AutoMigrate 会补齐该字段；数据库状态页说明改为以当前连接数据库实际结构检查为准。

- `/pricing` 删除 KYC 认证区域，改为深色点状背景和订阅卡片风格；后台自定义套餐数量、名称、描述、价格、额度和推荐状态会自动适配卡片展示。

- 套餐购买页未登录也能显示默认套餐；如果套餐表为空，访问套餐列表会自动补齐 GO、Plus、Pro、Max 默认套餐。
- 管理后台系统设置页不再因为登录来源统计失败而阻断进入；数据库页面会额外展示关键缺失字段，后台数据库类错误会返回更明确提示。

- Google OAuth 品牌一致性待测：公开页面、页面标题、登录页标识、后台品牌、默认隐私政策/服务条款、邮件模板站点名和新建工作流默认名称统一展示为 `Aivro`。
- 登录页 MetaMask 登录按钮改用本地图标兜底；账号密码注册调整为“邮箱和密码 -> 邮箱验证码 -> 个人/公司与名称”的三步流程，首次发送验证码前先完成人机验证并检查邮箱是否已注册，60 秒后再次发送需要重新人机验证。
- 第三方 OAuth 首次登录后直接进入账户信息补全页，不需要邮箱验证码；MetaMask 首次登录仍需要邮箱验证码，登录后再补全个人/公司与名称。
- 用户表新增 `account_type` 和 `profile_completed` 字段，启动数据库更新会补齐旧用户默认个人类型，并把已有昵称的旧用户标记为已完成资料。
- 登录、注册、找回密码和 MetaMask 邮箱验证开启 Turnstile 后，点击提交或发送验证码会先在屏幕中央弹出仅虚线框包裹的 Cloudflare Turnstile，通过后自动关闭并继续请求；未通过时请求不会发送到登录/注册/找回密码接口。
- 登录、注册等认证提交过程中的 Aivro Anime.js 加载动画去掉背景遮罩和卡片，只保留动画字和状态文字。
- 登录、注册、找回密码、顶部套餐入口、套餐购买页和 KYC 相关主要文案在中文/英文切换后展示对应语言。
- SMTP 测试发送支持 465 端口隐式 TLS，中文邮件标题按 MIME 编码发送。
- 生图和视频生成记录如果未登录或生成结果未转存为云端文件，会明确提示无法同步云端生成记录，不再静默跳过。
- 安全修复待测：默认管理员弱密码不再允许新建或登录，管理员密码必须使用更安全配置。
- 安全修复待测：在后台系统设置私有配置中启用 Turnstile 后，登录、注册、发送邮箱验证码、重置密码、MetaMask 登录和管理员登录必须完成人机验证。
- 安全修复待测：修改用户密码或重置密码后旧 JWT 应立即失效，禁用用户仍应无法通过旧 token 访问接口。
- 安全修复待测：OAuth 登录 state 绑定 HttpOnly nonce cookie，`//evil.com` 等协议相对 redirect 会回退到首页。
- 安全修复待测：MetaMask 登录必须验证签名地址与提交钱包地址一致。
- 安全修复待测：邮箱验证码失败次数超过限制后需要重新获取验证码。
- 安全修复待测：Didit webhook 未配置签名密钥或签名错误时拒绝处理，未知 session 不再新建 KYC 奖励记录。
- 安全修复待测：AI 返回图片 URL 转存时拒绝内网、回环和非公开地址。
- 安全修复待测：管理员调整用户算力点日志会记录操作者信息到 `extra`。
- 安全修复待测：前端 TypeScript 构建不再忽略类型错误，开发来源不再使用通配。
- 本地工作流持久化已移除待测。
- 更新镜像后服务启动自动执行数据库更新待测。
- 工作流列表从云端加载待测。
- 工作流创建次数不足拦截待测。
- 云端工作流只能访问自己数据待测。
- 删除导入/导出 zip 能力待测。
- 工作流分享生成链接待测。
- 已分享工作流再次点击分享会更新快照待测。
- 分享密码访问待测：旧 `/share/workflows/{token}` 链接和新公开分享路径的密码输入框、进入按钮需要上下分离，验证后进入复用画布视觉的只读预览。
- 分享访问权限待测：旧 `/share/workflows/{token}` 链接仍走登录页流程；新 `/{username}/{workflowName}` 公开路径允许未登录只读预览，Star 和 Fork 需要登录。
- 复制为独立工作流待测。
- 复制为跟随主工作流更新待测。
- 原作者更新分享后 linked 工作流自动更新待测。
- 套餐购买和 Stripe webhook 发放额度待测。
- KYC 通过发放奖励待测。
- `/privacy` 隐私政策和 `/terms` 服务条款页面改为纯黑背景、简洁文档排版，并移除旧品牌前缀展示。
- 管理后台系统设置“页面设置”页签支持分别编辑隐私政策和服务条款的中文、英文标题与正文，并可一键恢复默认内容。
- 管理后台系统设置的公开配置新增“页面访问控制”，可分别控制工作流、生图工作台、视频创作台、提示词库、我的素材是否需要登录访问；开启后未登录访问对应页面会跳转到登录页。
- 顶部导航栏桌面端将工作流、生图工作台、视频创作台、提示词库、我的素材居中展示，登录、主题和语言入口位置保持在右侧。
- 登录/注册页新增“登录/注册 Aivro，即代表同意隐私政策和服务条款”提示，链接到对应页面；登录/注册切换和输入区域新增过渡动画。
- 登录/注册、找回密码、MetaMask 邮箱绑定和 `/image` 生图按钮的 loading 图标改回 Ant Design 默认旋转图标；生成中结果卡片仍保留 Anime.js `createDrawable` 的 Aivro 线稿加载动画，线稿字形替换为明确的 `Aivro` 字样并支持浅色/深色主题。
- 生图工作台点击开始生成后，左侧记录区会立即出现仅展示 Aivro 线稿动画的生成中任务卡；任务完成后才展示提示词、成功数、张数、耗时和时间等信息。点击历史记录只预览已生成结果，不再回填提示词和参数，也不再提供失败结果重试入口，避免已生成任务被再次生成。
- 管理后台系统设置新增“云存储”页签，支持 Cloudflare R2 / S3、Endpoint、Region、Access Key、Bucket、Public Base URL、图片/视频路径模板、图片/视频过期天数、自动清理和 Path Style Endpoint 配置，并提供测试连接按钮；Secret Access Key 留空表示沿用已保存密钥。
- 新增 `cloud_files` 表记录云端生成文件；开启云存储后图片生成和视频生成会由后端转存到 R2/S3，前端展示、预览、保存素材、下载和生成历史使用云端 `public_url`，不再把生成结果写入浏览器本地图片/视频存储。
- 新增 `generation_histories` 表，生图工作台和视频创作台的生成历史改为保存到数据库；历史记录只保存已关联 `cloud_files` 的云端图片/视频，到期、删除或媒体不存在时列表接口会自动移除对应历史。
- 用户侧 API 配置入口已删除，前端不再允许用户填写 Base URL、API Key 或选择本地直连；图片、视频、文本和画布生成均只使用后台管理员配置并公开开放的模型。
- 云存储图片和视频分别按各自配置写入 `expires_at`，默认均为 7 天；自动清理任务会删除到期且未删除的云端对象并标记 `deleted_at`。
- 新增基础 i18n 能力，支持 `zh-CN` 和 `en-US`；首次进入按浏览器语言选择，手动切换后写入本地缓存并优先使用缓存语言，新增云存储文案提供中英文翻译。
- SEO 与本地化路径继续完善待测：公开页面使用 `/zh-CN`、`/en-US` canonical/hreflang/sitemap，登录、找回密码、资料补全、支付成功、MetaMask 邮箱绑定、我的素材、工作流和分享页改为 noindex 或 robots 排除，服务端初始 `html lang` 跟随 URL 语言。
- 顶部语言按钮改为下拉菜单，可选择中文和 English；切换后会同步 Ant Design 组件语言、页面 `lang`、顶部导航、移动端导航和首页主要文案。
- 新增全局英文运行时翻译层，切换 English 后会翻译现有用户页、画布、后台、弹窗、提示、placeholder、title 和 aria 文案；后台顶部新增语言切换入口，隐私政策和服务条款默认正文提供英文版本。
- 顶部导航新增“提示词反推 / Prompt Reverse”外链入口，跳转到 `https://insigh.aivro.org`。
- 用户侧和管理后台底部新增灰色版权声明 `Copyright © 2026 Maishan Inc. All rights reserved Aivro`。
- 英文模式下运行时翻译不再替换单字中文，也不会覆盖已经由组件按语言渲染的英文文案，避免出现 `Medium文`、`and` 混入中文等问题。
- Next 根布局强制动态渲染，页面、RSC 请求、`/api/*` 和文件内容响应新增多层 `no-store` 缓存头，避免 CDN 缓存 HTML、后台页面、配置接口和用户数据导致页面停留旧版本；`/_next/static/*` 等带 hash 的构建静态资源仍允许按平台默认长期缓存。
- 顶部导航去掉版本按钮和 GitHub 按钮，用户侧“我的画布”入口文案改为“工作流”，工作流列表、空状态、创建、导入、删除和清空等主要操作文案同步调整。
- 管理后台系统设置新增“邮件配置”页签，支持配置 SMTP 验证码、验证码有效期、注册绑定邮箱、找回密码和 MetaMask 邮箱验证邮件模板，并在后台提供模板预览。
- 管理后台邮件配置改为左右分栏：左侧配置 SMTP 并支持填写邮箱测试发送，右侧模板以块展示，点击后在大弹窗中左侧编辑模板、右侧实时预览；邮件模板新增 `{{ip}}`、`{{country}}`、`{{region}}` 变量。
- 管理后台新增“数据库配置”独立页面，可查看数据库结构是否完整、执行来源文件、缺失模型和每次更新记录；系统设置页不再放数据库更新按钮。
- 公开配置新增“是否开启邮箱验证”，开启后账号密码注册需要填写邮箱和验证码；新增找回密码页面，通过邮箱验证码重置密码。
- 管理后台系统设置新增“第三方登录”页签，Linux.do 登录迁移到该页签，并新增 Google、GitHub、MetaMask 和自定义 OAuth 登录配置，默认保留 O2 自定义登录项。
- 管理后台第三方登录配置为 Google、GitHub、MetaMask 补充登录图标，和 Linux.do 的图标展示保持一致。
- 管理后台第三方登录改为卡片列表，卡片展示开启状态和使用人数，点击后在弹窗中编辑；新增自定义第三方登录会创建新卡片。
- Docker 编排移除 PostgreSQL 的宿主机端口映射，数据库只允许应用容器通过内部网络访问。
- 登录页新增 Google、GitHub、Linux.do、自定义 OAuth 和 MetaMask 登录入口；MetaMask 签名后进入单独邮箱验证页面，验证后记录钱包地址并完成登录。
- 登录页第三方登录区域待测：Google、MetaMask 等按钮增加分隔标题和垂直间距；已绑定 MetaMask 钱包签名后直接登录，首次钱包登录的签名载荷改为 sessionStorage 中转，不再出现在邮箱绑定页 URL。
- 全站旧 `motion/react` 运行时动画替换待测：首页、登录页、Aivro 字标、生成 loading、文字 reveal 和画布助手面板改为 anime.js 动画，并支持 `prefers-reduced-motion` 降级。
- 用户管理适配第三方登录信息，展示登录来源、第三方 ID、MetaMask 钱包地址和邮箱验证状态；第三方头像会作为用户头像保存并展示。
- 修复删除画布图片节点或清空画布后撤销时，节点信息恢复但本地图片数据已被清理导致图片丢失的问题。
- “我的素材”类型筛选区右侧新增文本样式的导出素材和导入素材入口，可将全部素材导出为包含 `assets.json` 与图片、视频文件的压缩包，并从压缩包恢复素材。
- 未登录状态下，画布右上角不再显示用户头像菜单、用户名称、算力点余额和退出登录入口，改为显示登录入口；快捷键入口仍可直接打开。
- 生图工作台的图片参数区复用画布里的紧凑版图像设置面板，尺寸、质量、生成张数的交互保持一致；工作台仍保留独立的模型选择。
- 生图工作台生成记录改为数据库持久化：每次生成会保存提示词、参考图、模型、质量、尺寸、张数和云端图片信息；点击历史记录会回填本次生成配置并预览结果。
- 生图和视频生成记录默认保存 7 天，左侧记录区新增 Free 用户最多保存 7 天提示；记录卡片增加生成图片缩略图或视频预览。
- 上传图片、剪贴板图片、参考图和本地视频统一上传到后端文件接口；开启 R2/S3 时写入云存储，未开启时写入服务器本地目录，上传入口限制 5MB 并校验安全文件类型。
- 生成历史保存时会绑定生成媒体和参考图；删除历史或历史过期清理时会同步删除关联文件，R2/S3 和服务器本地存储共用同一套删除逻辑。
- 工作流保存时会把引用文件绑定为工作流文件并取消 7 天过期；删除工作流时会同步删除关联文件。
- 用户主题、语言和 AI 参数偏好改为保存到云端偏好接口，登录后自动读取应用，不再依赖浏览器本地偏好缓存。
- 素材导出读取后端文件内容；导入素材包时会重新上传图片和视频，并替换为新的后端文件引用。
- 视频设置抽成画布和视频创作台共用的紧凑面板，清晰度、尺寸、秒数按固定网格选择并支持手动输入；尺寸选择 `auto` 时请求不传 `size`。
- 修复画布和生图工作台选择图片尺寸后，请求图片生成/编辑接口未携带 `size` 参数的问题；`auto` 不传，其余比例或像素尺寸会随请求发送。
- 修复生图工作台和画布生图请求中 `quality` 参数可能传入上游不支持值导致 400 的问题；请求前会归一化质量枚举，`auto` 或异常值不再发送给上游。
- 管理后台新增/编辑渠道时，渠道可用模型支持通过弹窗按“新获取、已有”分组选择，并可在弹窗内手动增加模型或拉取模型列表后再写回表单。
- 管理后台编辑渠道时，API Key 留空不再触发必填校验，表示沿用已保存的密钥；新增渠道仍要求填写 API Key。
- 管理后台公开配置里的系统可用模型候选项改为由已启用渠道中选择的模型合并去重生成，最终开放哪些模型仍由公开配置里手动勾选。
- 视频生成请求参数对齐 `grok-imagine-video` 接口：使用 `resolution_name`、`preset=normal`、`input_reference[]`，支持清晰度、尺寸、秒数快捷选择和手动输入，并支持最多 7 张参考图。
- 画布视频设置浮层改为挂载到页面根层级并使用自建浮层交互，避免被节点悬浮工具栏遮挡或点击面板内容时关闭。
- 画布生成配置节点的生图参数改为复用图像设置浮层，支持在同一个入口里调整质量、尺寸和生成张数。
- 视频清晰度输入框改为只输入数字，提交请求时再拼接 `p` 单位。
- 视频生成前端会识别后端 `{ code, msg }` 错误响应，创建失败不再继续轮询 `undefined`。
- 新增 `/video` 视频创作台页面，参考生图工作台布局，支持提示词、参考图、视频参数、生成结果、保存素材、下载和数据库生成记录；清晰度和秒数均支持常用值选择与手动输入，生成记录保存云端媒体信息并可回填本次提示词、参考图和参数。
- 视频创作台生成前会把模型、尺寸、秒数、清晰度归一化为视频接口支持的参数，并展示后端返回的错误信息，避免页面侧残留的生图参数影响视频请求。
